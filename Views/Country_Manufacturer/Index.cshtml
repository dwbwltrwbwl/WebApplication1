@model IEnumerable<WebApplication1.Models.Country_Manufacturer>

@{
    ViewData["Title"] = "Index";
}

<h1 style="color: #94a728;">Страны производителей</h1>

<table class="table">
    <thead></thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.countryManufacturer)
            </td>
        </tr>
}
    </tbody>
</table>
