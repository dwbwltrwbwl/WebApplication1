@model IEnumerable<WebApplication1.Models.Category>

@{
    ViewData["Title"] = "Index";
}

<h1 style="color: #94a728;">Категории</h1>

<table class="table">
    <thead></thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.category)
            </td>
        </tr>
}
    </tbody>
</table>
