@model IEnumerable<WebApplication1.Models.Material>

@{
    ViewData["Title"] = "Index";
}

<h1 style="color: #94a728;">Материалы</h1>

<table class="table">
    <thead></thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.material)
            </td>
        </tr>
}
    </tbody>
</table>
