@model WebApplication1.Models.Firm

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Firm</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.firm)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.firm)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.idFirm">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
