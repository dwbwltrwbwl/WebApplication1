@model IEnumerable<WebApplication1.Models.Firm>

@{
    ViewData["Title"] = "Index";
}

<h1 style="color: #94a728;">Фирмы</h1>

<table class="table">
    <thead></thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.firm)
            </td>
        </tr>
}
    </tbody>
</table>
